# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables — NUNCA subir credenciales
.env
.env.production
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# deploy
.netlify/
.vercel/

# local session artifacts
session-export-*.zip

# claude local settings
.claude/settings.local.json

# documentos locales pesados (quedan en disco, no en el repo)
sitemap.pdf

# Copias que crea el sync de ~/Documents ("archivo 2.sql", "archivo 2.json"). Son
# idénticas al original y no aportan nada; commitearlas hace que una migración figure
# dos veces y que alguien la corra dos veces por las dudas.
* 2.*
* 3.*
