---
// /book/ — página pública de booking. Toda la UI vive en BookingApp.astro (compartida
// con /book/[persona]). preHost=null → si hay 2+ hosts visibles muestra el selector;
// con 0 o 1 visible se comporta idéntico a hoy (agenda default).
import BookingApp from '../../components/BookingApp.astro';
---
<BookingApp preHost={null} />
